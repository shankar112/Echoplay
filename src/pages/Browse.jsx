import React from "react";
import { Typography } from "@mui/material";

export default function Browse() {
  return <Typography variant="h5">Browse (stub) — build categories, playlists here.</Typography>;
}
