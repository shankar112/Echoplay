import React from "react";
import { Typography } from "@mui/material";

export default function Library() {
  return <Typography variant="h5">Library (stub) — liked songs & playlists.</Typography>;
}
